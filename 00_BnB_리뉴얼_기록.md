# BnB 스터디 홈페이지 리뉴얼 기록

날짜: 2026-08-16
위치: `BnB_Homepage` / 로컬 `C:\Users\Luna\bnb-homepage`

## 한 줄 결론

그누보드 기반 [bnbstudy.co.kr](http://bnbstudy.co.kr/)을 Next.js + Tailwind로 다시 짠 **모던 테크 커뮤니티형 포털**을 만들었습니다.

## 왜 이 구조인가

- 신입생은 홈페이지 가입(계정)과 스터디 입회(구글폼)를 따로 하게
- 정회원은 일정 → 자료실 → 소모임 → 혜화동 아지트로 흐르게
- 계정은 Firebase Auth, 회원/시간표는 Firestore, 운영은 `/admin`

## 반영한 기존 사이트 정보

- 슬로건, 혜화동 아지트(대여/예약 없음), 38기, 과목 분담 강의
- 카카오채널 `https://pf.kakao.com/_exoxhBX` / 1:1 상담 `https://pf.kakao.com/_exoxhBX/chat`
- 입회원서 구글폼 `https://docs.google.com/forms/d/e/1FAIpQLSe7e60br0OGZOX532hXDDcS1VO6-gJWIKv2tD-G2otvVE-qng/viewform`
- 회비: 학기 16만 / 연납 30만 / 가입비 3만
- 계좌: 카카오뱅크 3333-38-0133113 손진호
- 1990년 창설 연혁, 2025 개정 회칙 요약
- 기존 공지/자료실/소모임/갤러리 제목

## 아직 하지 않은 것 (다음 단계)

1. 입회원서는 공식 구글폼 유지. 제출 알림을 운영진 메일로 붙일지는 나중에
2. 아지트 실제 사진 교체
3. 학습일정 팝업의 공식 시간표를 운영진 확인 후 데이터 교체
4. 그누보드 포인트·접속자로그·대량메일, 기존 회원 이관. Auth 계정 완전 삭제는 Cloud Functions(Blaze)가 필요해 지금은 탈퇴 상태로만 막는다
5. Firebase 콘솔에서 Storage “시작하기”를 한 번 켜야 자료 파일이 올라간다. 위치는 Firestore와 같이 `asia-northeast3`가 좋다

## 변경 기록

### 2026-08-17 — 아지트 소개 문구 정리

- `/about/room` 히어로에서 “외부에 빌려 주는 스터디카페가 아니라” 문장을 뺐다.
- 제목이 이미 “우리 아지트”라서, 부정 설명은 중복이고 첫인상만 길어진다.
- 위치(`SITE.locationDetail`)와 “학우들이 매주 모이는 집”만 남겼다.
- 실제 실행 중인 로컬 경로 `C:\Users\Luna\bnb-homepage`에도 같은 수정을 반영했다. 구글 드라이브 워크스페이스만 고치면 화면이 안 바뀐다.

### 2026-08-17 — 관리자 페이지와 회원 DB 연동

- Firebase 프로젝트 `bitandbyte-homepage`, Auth 이메일/비밀번호, Firestore(서울 Standard)
- 가입 DB 위치: Firestore `members/{uid}`
- 아이디 찾기용: `loginAliases/{loginId}` (get만, 목록 조회 금지)
- 관리 주소: `/admin` (회원 등업), `/admin/schedule` (수업 변경)
- 역할: `site` 사이트 회원, `study` 정회원, `admin` 운영진
- 첫 마스터 계정: 아이디 `bnbstudy` (역할 `admin`). 비밀번호는 대화로만 전달했고 기록 파일에는 적지 않는다
- 2026-08-17 로그인 실패는 Firestore 규칙이 콘솔에 안 올라가 아이디 조회가 막혀서였다. CLI로 규칙을 다시 배포해 해결했다
- 2026-08-17 시간표 관리: 강의실은 실습실/강의실 선택, 과목은 사전 등록 후 선택, 강사 이름 추가
- 입회원서는 구글폼 유지. 입금 확인 후 `/admin/members`에서 정회원으로 올린다
- 브라우저 localStorage 계정은 더 이상 쓰지 않으므로 학우는 다시 가입해야 한다
- 2026-08-17 로컬 서버 실행기 `BnB-Homepage.exe` 추가. 더블클릭하면 npm run dev 후 localhost:3000을 연다
- 포트 3000이 이미 켜져 있으면 오류 대신 브라우저만 열도록 수정했다
- 2026-08-17 홈페이지가 `로드하는 중...` 흰 화면으로 멈춘 이유: 전날부터 떠 있던 Next 개발 서버가 포트만 잡고 HTTP는 응답하지 않았다. 프로세스를 종료하고 `npm run dev`를 다시 켜 200 응답을 확인했다
- EXE는 이제 포트가 열려 있는지만 보지 않고, 실제 페이지가 응답하는지 확인한다. 응답이 없으면 멈춘 프로세스를 끄고 다시 시작한다
- 2026-08-17 과목 등록 분류에 `소모임`을 추가했다. 학년은 1~4, 소모임은 `club`로 저장하고 화면에는 `소모임`으로만 보여 준다. 기존 소모임 이름(AI 활용반 등)은 기본 목록에 넣었다
- 2026-08-17 강의 등록에 반복 주기(매주/격주/한번)와 시작일~종료일을 넣었다. 날짜마다 문서를 복제하지 않고, 강의 하나에 규칙을 저장한다
- 2026-08-17 수업 등록 폼을 짝 필드끼리 같은 줄에 묶었다. 시작일/종료일, 시작 시각/종료 시각이 떨어져 보이지 않게 했다
- 2026-08-17 회원 관리에 기수 칸을 넣었다. 2026년 기준 1학년=38기, 2학년=37기, 3학년=36기, 4학년=35기다. 학년을 고르면 기수가 채워지고, 편입·유급만 직접 바꾼다
- 2026-08-17 회원 학년에 OB를 추가했다. 졸업 선배도 가입할 수 있고, 기수는 본인 기수를 직접 고른다
- 2026-08-17 학습일정 달력에 반복 강의를 날짜로 펼쳐 표시한다. 매주/격주/한번 규칙으로 해당 요일에 점을 찍고, 날짜를 누르면 수업 카드가 나온다
- 2026-08-17 소모임 페이지와 수업 등록을 연동했다. `/admin/labs`에서 소개를 관리하고, 일정은 시간표에서 분류 `소모임`+같은 이름으로 연결된다
- 2026-08-17 소모임 관리 화면의 `lab-c` 중복 키 경고를 고쳤다. 기본 목록과 DB를 이름만으로 합치면, 이름을 바꾼 뒤에도 같은 ID가 두 줄로 남았다. 이제는 ID를 먼저 맞추고 같은 ID는 한 줄만 남긴다
- 2026-08-17 학습일정 달력에 하루짜리 안내(정규 강의 개강, OT, 출석수업)를 운영진이 직접 넣을 수 있게 했다. 주소는 `/admin/calendar`, 저장 위치는 Firestore `calendarEvents`. 매주 수업은 기존 `/admin/schedule`을 쓴다
- 공개 달력(`/academics/schedule`)은 이제 코드에 박힌 목록이 아니라 `calendarEvents`를 보여 준다. 서버가 비어 있을 때만 기본 샘플을 보여 주고, 첫 저장 때 그 샘플을 한 번 올린다
- Firestore 규칙에 `calendarEvents` 읽기(공개)/쓰기(운영진)를 넣고 CLI로 배포했다
- 2026-08-17 학사 일정에 종료일을 추가했다. `date`는 시작일, `endDate`는 마지막 날이다. 하루면 둘을 같게 두고, 기간이면 그 사이 날짜에 달력 점이 찍힌다. 예전 문서에 종료일이 없으면 시작일과 같은 하루로 읽는다
- 2026-08-17 오시는 길에 기존 사이트와 같은 주소·골목 안내·지하철·버스를 넣었다. 지도는 API 키 없이 OSM으로 붙이고, 크게보기/길찾기는 카카오맵 등록 장소(`비트&바이트`, 율곡로16길 5 2층)로 연다. 주소·교통 안내는 `SITE` 한곳에만 둔다
- 2026-08-17 자료실은 아직 기존 그누보드 게시글 제목 샘플만 보여 준다. 실제 파일·업로드·다운로드는 이관하지 않았다. 파일은 당분간 `bnbstudy.co.kr` 학년 자료실을 쓴다
- 2026-08-17 자료실에서 기존 샘플 제목을 뺐다. 공개 화면은 Firestore `resources`만 보여 주고, 운영진은 `/admin/resources`에서 파일을 다시 올린다. 파일은 Firebase Storage `resources/{id}/파일명`. Firestore 규칙은 배포했고, Storage는 콘솔에서 버킷을 아직 안 켜서 규칙 배포가 보류다
- 2026-08-17 회칙 페이지에 기존 사이트(`bnb.php#rule`) 제12차 개정(2025-12-14) 전문을 넣었다. 요약이 아니라 1~17조와 개정 연혁을 그대로 옮겼다
- 2026-08-17 홈 ‘이번 주 강의’는 끝난 수업만 가리는 게 아니라, 월~일 사이에 실제로 열리는 수업만 보여 준다. 개강 전이면 빈 안내가 나온다
- 2026-08-17 회원 관리를 그누보드처럼 선택/검색/상태(정상·차단·탈퇴)/회원추가로 넓혔다. 게시판 관리(`/admin/boards`)에서 공지·라운지·갤러리·Q&A·소모임·운영 게시판을 만들고, 공개 커뮤니티에서 글·댓글을 쓴다. Firestore 규칙을 배포했다. 포인트·SNS·Auth 완전 삭제는 넣지 않았다
- 2026-08-17 `/admin/schedule` 404는 페이지 파일이 없어서가 아니라, next.config 변경 뒤 Turbopack 캐시가 꼬인 탓이었다. `.next`를 지우고 개발 서버를 다시 켜면 200으로 돌아온다. 자료실 관리(`/admin/resources`)도 같이 404였다가 함께 복구됐다
- 2026-08-17 학사·스터디 일정에 반복(하루/기간/매주/격주)을 넣었다. 매주·격주는 시작일 요일을 기준으로 종료일까지 달력에 점을 찍는다. 예전 문서에 반복이 없으면 하루 또는 기간으로 읽는다
- 2026-08-17 학사 일정 입력칸에서 반복 안내 문구를 선택상자 밖으로 빼, 구분 칸이 세로로 늘어나지 않게 맞췄다
- 2026-08-18 기존 `qa` 게시판을 **신편입생 게시판**으로 바꿨다. 쓰기 등급 `guest`(모두)라 로그인하지 않아도 글·댓글을 남긴다. 주소는 `/community/qa` 그대로다.
- 비가입자는 표시 이름(1~20자)만 넣고, 파일 업로드는 막았다(공개 이미지 주소만). 작성자 ID는 `guest-숫자`라 본인이 나중에 수정·삭제하지 못한다. 삭제는 운영진 `/admin/boards/qa`.
- Firestore 규칙에 비로그인 글/댓글 생성을 넣었다. 운영진이 `/admin`에 한 번 들어가면 기존 `qa` 문서의 쓰기 권한이 `guest`로 맞춰진다.
- 공개 글쓰기라 스팸이 올라올 수 있다. 쓰기 등급 ‘모두’는 이 게시판에만 쓰는 것을 권한다.
- 2026-08-18 비가입자 글쓰기에 IP 차단을 넣었다. 기본은 해외 IP 거부(KR만 허용), 같은 IP 도배 시 하루 임시 차단, 운영진이 `/admin/security`에서 IP를 직접 막을 수 있다.
- 검사 위치는 Next 서버 `/api/guest-guard`다. Firestore 규칙만으로는 접속 IP를 볼 수 없다. 화면에서 글을 남기면 서버가 먼저 막지만, Firestore를 직접 두드리면 우회될 수 있다. 공개 서비스 전에는 Cloudflare 같은 앞단이 더 안전하다.
- IP는 공개 글에 남기지 않고, 서버 파일 `data/security-runtime.json`과 운영진 화면에만 둔다.
- 2026-08-18 사진 업로드 CORS 오류의 원인은 Storage 버킷이 아직 없던 것이었다. 콘솔에서 ‘시작하기’를 안 눌러서, 브라우저 preflight가 실패하고 CORS로 보였다. 서울(`asia-northeast3`) 기본 버킷 `bitandbyte-homepage.firebasestorage.app`을 만들고 규칙·CORS를 배포했다.
- 2026-08-18 이어서 나온 403은 로그인이 풀린 게 아니라 Storage 규칙이 `ownerUid`를 보고 거절한 것이었다. 커스텀 메타데이터 키는 소문자 `owneruid`로 저장된다. 규칙을 맞추고 운영진도 올릴 수 있게 배포했다.
- 2026-08-18 그래도 403이 남아, 사진 규칙에서 Firestore 조회·메타데이터 조건을 뺐다. 로그인한 사용자가 `image/*` 8MB 이하만 올리면 통과한다. 파일 이름도 한글/공백 없이 `시간-순번.확장자`로 단순화했다.
- 2026-08-18 커뮤니티 첫 화면을 큰 카드 나열에서 그누보드형 허브로 바꿨다. 위쪽에 바로가기 칩, 아래는 2열 위젯에 최근 글·글쓰기·더보기(+)를 넣었다. 갤러리는 썸네일 3장을 같이 보여 준다.
- 2026-08-18 사진 403의 실제 원인은 규칙만이 아니었다. 콘솔 ‘시작하기’ 없이 API로 버킷만 만들어서, Firebase Storage 서비스 계정이 버킷 IAM에 없었다. 서비스 계정을 만들고 권한을 준 뒤, 사진 규칙은 로그인 여부만 보게 단순화했다.
- 2026-08-18 글 상세 사진을 클릭하면 원본을 크게 띄우도록 했다. 배경·ESC로 닫고, 여러 장이면 화살표로 넘긴다. 필요하면 ‘새 탭에서 원본 열기’로 실제 파일도 볼 수 있다.
- 2026-08-18 자료실에 **소모임 · 기타** 칸을 넣고, 예전 쉐어노트처럼 태그로 올리고 찾는 기능을 붙였다. Flask/SQLite는 쓰지 않고 Firestore `shareNotes`와 Storage `share-notes/`를 쓴다. 올리기·열람은 로그인 회원, 삭제는 본인 또는 운영진이다. 예전 앱의 삭제 암호·관리자 비밀번호는 가져오지 않았다.
- 2026-08-18 자료실 학년 방을 누르면 같은 화면에서 걸러지지 않고, `/academics/resources/1`처럼 그 게시판으로 이동한다. 소모임·기타는 `/academics/resources/club`이다.
- 2026-08-18 쉐어노트를 소모임만이 아니라 1~4학년 방에도 넣었다. 노트에 `room` 값을 저장해서 방마다 따로 쌓인다. 예전 노트(방 없음)는 소모임으로 읽는다. 1~4학년에는 운영진 자료와 학우 노트가 같이 보인다.
- 2026-08-18 운영진 자료실 업로드 403은 Storage 규칙이 첫 운영진 이메일만 받아서였다. 관리 화면은 역할만 보므로, 다른 운영진 계정은 파일이 막혔다. 로그인 회원이면 올리게 바꿨고, 한글 파일명은 저장 경로만 영문으로 둔다. 목록에 올리는 권한은 여전히 Firestore 운영진 규칙이다.
- 2026-08-18 쉐어노트는 목록에서 제목을 누르면 게시글처럼 열린다. 글 안에 공부하기(PDF 화면), 다운로드, 노트 정리, 퀴즈 만들기를 나열했다. 정리·퀴즈는 서버 `GEMINI_API_KEY`가 있을 때만 동작한다.
- 2026-08-19 쉐어노트 글에 본문(`body`)을 넣었다. 쓰기 화면에 본문 칸이 있고, 글을 열면 내용이 보이며 작성자·운영진은 수정할 수 있다. 예전 글은 본문이 비어 있다.
- 2026-08-19 홈페이지 소스를 GitHub 전용 저장소에 올렸다. 주소는 `https://github.com/karin01/bnb-homepage`이다. git 루트는 `C:\Users\Luna\bnb-homepage`이다. `.env.local`은 올리지 않았다.
- 2026-08-19 포트폴리오처럼 GitHub Pages로 공개 주소를 만들었다. `https://karin01.github.io/bnb-homepage/`
- 2026-08-19 1학년 자료실에서 쉐어노트가 안 보이던 이유는 글이 없어서가 아니다. `멀티미디어 정리`는 Firestore `room: "1"`에 있는데, 화면 위쪽이 비어 있는 운영진 자료라 게시판이 빈 것처럼 보였다. 쉐어노트를 위로 올리고 자료실 첫 화면에도 보이게 했다.
- 2026-08-19 GitHub Pages에서 카카오채널 QR이 깨진 이유는 이미지 주소가 `/images/...`라서 `karin01.github.io` 루트를 찾기 때문이다. `/bnb-homepage`를 붙이도록 고쳤다.
- 2026-08-19 쉐어노트 ‘글 열기’와 뒤로가기가 안 먹힌 이유는 같은 방에서 `?note=`만 바뀌는데 화면이 주소를 다시 읽지 않아서이다. Next.js `useSearchParams`로 주소 변화를 구독하게 고쳤다.
- 2026-08-19 푸터에 학교 바로가기를 넣었다. 컴퓨터과학과, 개인학사정보, 방송대 홈, 서울지역대학, 중앙도서관이다. 학교 사이트는 새 탭으로 연다.
- 2026-08-19 푸터 학교 칸에 Q-Net(https://www.q-net.or.kr/)을 추가했다.
- 2026-08-19 푸터 카카오채널 QR에서 ‘휴대폰 카메라로 찍으면 채널이 열립니다’ 안내 문장을 뺐다. QR과 바로가기만으로 충분하다.
- 2026-08-19 자료실(쉐어노트) 업로드에 과목 선택을 넣었다. 학년/과목은 새로 만들지 않고 학습일정(`subjects`, `/admin/schedule`)과 같은 목록을 쓴다. 소모임 방은 `/admin/labs` 이름을 고른다. 예전 노트는 과목이 비어 있으면 ‘과목 미지정’으로 보이고, 고칠 때 고르면 된다. 과목 목록을 회원이 읽을 수 있게 Firestore `subjects` 읽기를 공개로 바꿨다.

- 2026-08-19 자료실 첫 화면에서 로그인이 되어 있는데도 ‘로그인 후 글쓰기’가 나온 이유는, 글쓰기를 ‘특정 학년 방 안’에서만 열어 두었기 때문이다. 방이 없으면 로그인 여부와 상관없이 그 문구가 나왔다. 이제는 로그인한 회원이면 자료실 전체에서도 ‘노트 쓰기’가 보이고, 올릴 방을 폼에서 고른다.
- 2026-08-19 GitHub Pages `https://karin01.github.io/bnb-homepage/` 를 실제 확인 주소로 쓴다. `main`에 푸시하면 Actions가 정적 사이트를 다시 올린다. localhost 저장만으로는 Pages가 바뀌지 않는다.
- 2026-08-19 다크모드에서 학년/과목 선택 상자 글자가 안 보이던 이유는, 펼친 목록은 흰 배경인데 글자는 밝은 테마 색을 물려받기 때문이다. `color-scheme`과 선택지 배경을 테마에 맞춰 사이트 전체 입력칸을 고쳤다.
- 2026-08-19 게시판 관리 표가 지저분해 보인 이유는 읽기/쓰기·체크·관리 버튼을 한 칸에 세로로 쌓아서, 줄 높이가 제각각이었기 때문이다. 칸마다 입력 하나만 두고 높이를 맞춰 가로 정렬했다.

## 실행

가장 쉬운 방법: `C:\Users\Luna\bnb-homepage\BnB-Homepage.exe` 더블클릭

```bash
cd C:\Users\Luna\bnb-homepage
npm install
npm run dev
```

관리 콘솔: https://console.firebase.google.com/project/bitandbyte-homepage/overview
